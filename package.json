{
  "name": "wiki",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "local:cms": "npx @staticcms/proxy-server",
    "local:serve": "browser-sync .",
    "start": "npm-run-all --parallel local:cms local:serve"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^2.0.1",
    "browser-sync": "^2.29.3",
    "browsersync": "^0.0.1-security",
    "npm-run-all": "^4.1.5"
  }
}
